{
  "name": "@bitbaum/site",
  "private": true,
  "version": "0.0.0",
  "description": "The studio site. A generator and a stylesheet — the only dependency is the design SSOT the products already use.",
  "type": "module",
  "scripts": {
    "build": "node site/build.mjs",
    "check": "node site/build.mjs --check",
    "test": "node --test site/*.test.mjs",
    "publish:site": "bash site/publish.sh"
  },
  "dependencies": {
    "@bitbaum/design-tokens": "1.1.0"
  }
}
